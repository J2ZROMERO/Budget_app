<p style="color: green"><%= notice %></p>

<h1>Transactions</h1>



<h2>Total Amount </h2><%= @budgets.sum {|p| p.budget.amount}  %>  



<div id="budgets">
<% @budgets.each do |budget| %>
  <%= render "budget",budget: budget %>
    <p>
    <%= budget.budget.amount %>
    </p>
  <% end %>
</div>
<%= link_to "Back to budgets", groups_path(params[:group_id]) %>
<%= link_to "New budget", new_group_budget_path %>
